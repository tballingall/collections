- if (@user, @album).errors.any?
  #error_explanation
    .alert.alert-danger
      The form contains #{pluralize(@user.errors.count, "error")}.
    %ul
      - (@user, @album).errors.full_messages.each do |msg|
        %li= msg